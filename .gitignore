node_modules/
.next/
.env.local
.env*.local
.DS_Store
*.log
.vercel

# One-off development helper scripts (not for production)
*.sh
docs/

# TypeScript incremental build cache (local-only)
*.tsbuildinfo

# Build/migration scripts (local only)
*.sql
*.mjs
*.py
